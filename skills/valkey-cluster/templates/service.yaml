apiVersion: v1
kind: Service
metadata:
  name: ${name}-headless
  namespace: ${namespace}
spec:
  clusterIP: None
  selector:
    app: ${name}
  ports:
    - port: ${port:6379}
      targetPort: ${port:6379}
      name: client
    - port: 16379
      targetPort: 16379
      name: gossip
